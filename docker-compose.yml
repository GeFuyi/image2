services:
  image2-proxy:
    build: .
    container_name: image2-proxy
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
